<% const yuxuan = lang == 'zh' ? '林榆璇' : 'Yuxuan Lin' %>

<!DOCTYPE html>
<html lang="<%= lang %>">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%- include('../partials/head') %>
  <title><%= yuxuan %> | Contact</title>
</head>

<body>
  <%- include('../partials/header') %> <%- include('../partials/nav') %>

  <main name="contact">
    <div class="wrapper flex column gap">
      <h2 class="header fourth-header"><%= lang == 'zh' ? '联系我' : 'Contact Me' %></h2>
      <form action="/nodemailer" method="post" class="primary-form">
        <div class="item">
          <div class="subitem">
            <label for="name">
              <%= lang == 'zh' ? '姓名*' : 'Name*' %>
            </label>
            <input type="text" name="name" required />
          </div>
          <div class="subitem">
            <label for="email">
              <%= lang == 'zh' ? '邮箱*' : 'Email*' %>
            </label>
            <input type="text" name="email" required />
          </div>
        </div>
        <div class="item">
          <div class="subitem">
            <label for="subject">
              <%= lang == 'zh' ? '主题*' : 'Subject*' %>
            </label>
            <input type="text" name="subject" required />
          </div>
        </div>
        <div class="item">
          <div class="subitem">
            <label for="message">
              <%= lang == 'zh' ? '信息' : 'Message' %>
            </label>
            <textarea name="message" id="" cols="30" rows="10"></textarea>
          </div>
        </div>
        <div class="item">
          <div class="subitem">
            <label for="auth">2 + 1 = ?</label>
            <input type="text" name="auth" required />
          </div>
        </div>
        <div class="item">
          <div class="subitem">
            <button type="submit" class="btn btn-primary">
              <%= lang == 'zh' ? '发送' : 'Send' %>
            </button>
          </div>
        </div>
      </form>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>

</html>